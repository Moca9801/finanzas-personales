/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./src/**/*.{html,ts}",
    ],
    theme: {
        extend: {
            colors: {
                // Custom colors based on prompts can be added here
                // Using standard tailwind colors for now
            },
        },
    },
    plugins: [],
}
